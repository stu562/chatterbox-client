var Rooms = {
    $room: $(".room"),
    add: function() {
        //adds rooms
        Rooms.$room.on('click');
            //`<option value="lobby">${some text inside add room textbox}</option>`
    }
};